name : Hola Mundo

on : [push]

jobs:
  hola:
    runs-on: ubuntu-latest

    steps:
      - name: Hola mundo
        run: echo "¡Hola, Mundo!"

  other:
    runs-on: ubuntu-latest

    steps:
      - name: LS
        run: echo "ls -la" && ls -la